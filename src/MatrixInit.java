import java.util.Random;

public class MatrixInit {
    private static Random random = new Random();

    public static void FillMatrix(Matrix m, int nonzero, int maxvalue){
        random.nextInt(maxvalue);

    }
}
